[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[workspace]
members = [
    "crates/agent",
    "crates/server",
    "crates/protocol",
    "crates/pulse-config",
]
resolver = "3"
