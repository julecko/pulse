[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }
pulse-config = { path = "../pulse-config" }
serde = { version = "1.0.229", features = ["derive"] }
sysinfo = "0.38.4"
tracing = "0.1"
