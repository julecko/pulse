[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
pulse-shared = { version = "0.1.0", path = "../pulse-shared" }
serde = { version = "1.0.229", features = ["derive"] }
tracing = "0.1.44"
